#include <iostream>
#include <vector>

using namespace std;

/**
Use Backtracking to Increment the entries of a matrix,
where vertical and horizontal entries are 1
*/



int main()
{
    cout << "Increment the values of a Matrix where the entry meets some condition!" << endl;

    vector<vector<int>> board = {{1,0,1,2}, {0,0,0,1}, {1,1,1,1}};
    return 0;
}
